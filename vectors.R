# creating a vector using : operator
x <- 1:7; x

# create a vector with a range
y <- 2:-2; y

# creating the vector with the seq() function
seq(1, 3, by=0.3)

# specify the length of the vector
seq(1, 5, length.out=4)

# access the 2nd element in x--> pass the element number
x[2]

# access two element in x vector
x[c(2, 5)]

# get all elements except 2nd element
x[-2]
